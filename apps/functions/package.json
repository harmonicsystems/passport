{
  "name": "@market-passport/functions",
  "version": "0.1.0",
  "private": true,
  "main": "lib/index.js",
  "scripts": {
    "build": "tsc",
    "build:watch": "tsc --watch",
    "serve": "npm run build && firebase emulators:start --only functions",
    "shell": "npm run build && firebase functions:shell",
    "deploy": "firebase deploy --only functions",
    "lint": "tsc --noEmit",
    "clean": "rm -rf lib"
  },
  "engines": {
    "node": "20"
  },
  "dependencies": {
    "@market-passport/shared": "workspace:*",
    "firebase-admin": "^12.0.0",
    "firebase-functions": "^4.5.0",
    "jsonwebtoken": "^9.0.2",
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "@types/jsonwebtoken": "^9.0.5",
    "typescript": "^5.3.3"
  }
}
